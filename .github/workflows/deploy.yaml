name: Deploy Periodic Table

on:
  push:
    branches: [ "main" ] # Or your main branch name
  pull_request:
    branches: [ "main" ]

permissions:
  pages: write      # Required to deploy to GitHub Pages
  id-token: write   # Required for the deployment action to authenticate

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          override: true

      - name: Build Rust project
        run: cargo build --release
        working-directory: /home/runner/work/periodic-table/periodic-table

      - name: Generate index.html
        run: ./target/release/periodic-table # Ensure this matches your built executable name
        working-directory: /home/runner/work/periodic-table/periodic-table

      - name: Upload GitHub Pages artifact
        uses: actions/upload-artifact@v4
        with:
          name: github-pages
          path: /home/runner/work/periodic-table/periodic-table

      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          # The deploy_branch and folder are no longer directly used by this action in v4
          # as it expects the artifact.
